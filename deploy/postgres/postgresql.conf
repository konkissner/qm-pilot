# PostgreSQL settings for QM-Pilot (mounted into container)
listen_addresses = '*'
port = 5432
timezone = 'UTC'
log_timezone = 'UTC'
shared_preload_libraries = ''

# Connections only from docker network (no host publish in prod compose)
max_connections = 100
